interface:
  display_name: "Hive"
  short_description: "Repo-specific patterns and workflows for hive"
  default_prompt: "Use the hive repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true